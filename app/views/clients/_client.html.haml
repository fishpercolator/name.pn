%tr.api-client
  %td= client.name
  %td= t('ago', time: time_ago_in_words(client.created_at))
  %td
    = button_to client_path(client), method: :delete, class: 'btn btn-error btn-sm', form: {data: {turbo_confirm: t('.confirm_delete')}}  do
      = icon 'delete'
      = t('actions.delete')
